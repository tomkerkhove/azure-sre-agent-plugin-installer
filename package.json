{
  "name": "azure-sre-agent-plugin-installer",
  "version": "1.0.0",
  "private": true,
  "description": "Static site that helps plugin authors add an 'Install to Azure SRE Agent' badge to their README.",
  "scripts": {
    "test": "jest",
    "test:e2e": "playwright test",
    "serve": "node scripts/serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
